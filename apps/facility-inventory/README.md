# Реестр оборудования объекта (`facility-inventory/`)

Реестр установленного на объекте оборудования. Модуль в стадии заготовки (stub) — экран-заглушка без рабочей функциональности.

- **Тип:** `ui`
- **Точка входа:** `index.html`
- **Главные файлы:**
  - `index.html` — страница-заглушка (stub) с описанием будущего реестра
- **Расчётная часть (calc):** —
- **UI/рендер:** `index.html` (stub)
- **Данные/справочники:** — (нет manifest.json; не зарегистрирован в modules.json)
- **Cross-module связи:** нет
- **Куда добавлять новое:** при реализации — добавить manifest.json и запись в `modules.json`; экран и модель данных реестра — в этом каталоге
