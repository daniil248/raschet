// =========================================================================
// SHIM (v0.60.522, Фаза 2 модуляризации — burndown allowlist R1).
// Реализация перенесена в SHARED-слой: shared/meteo-util.js (generic
// утилиты: newId/computeStats/escHtml/escAttr/modalOpen/toast/
// readFileAsText — используются meteo, cooling, service). Этот файл
// оставлен как совместимостный re-export, чтобы существующие
// относительные импорты `./util.js` внутри модуля meteo/ продолжали
// резолвиться (zero-build, без переписывания путей). Только named
// экспорты (default отсутствует) → `export *` полностью прозрачен.
// Не удалять, пока есть импортёры старого пути.
// =========================================================================
export * from '../shared/meteo-util.js';
