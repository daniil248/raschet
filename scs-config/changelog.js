// scs-config/changelog.js — журнал изменений модуля «Конфигуратор СКС».
// Новые записи сверху.

export const MODULE_CHANGELOG = [
  {
    version: '0.1.21',
    date: '2026-04-22',
    items: [
      'Phase 1.26 — единый дизайн всех модулей (см. ROADMAP 1.26.1-3). В <code>shared/styles/base.css</code> добавлены глобальные правила: (1) полноэкранное отображение — reset <code>max-width/margin:0 auto</code> на всех wrapper-классах (<code>.sc-wrap, .rc-wrap, .cb-wrap, .mv-wrap, .pdu-wrap, .ups-wrap, .mdc-wrap, .bat-wrap, .cat-wrap, .log-wrap, .psy-wrap, .sup-wrap, .pc-wrap, .tr-wrap, .app-wrap, .page-wrap, body > main</code>) через <code>!important</code>; (2) единая типографика — <code>html{font-size:14px;line-height:1.45}</code> и <code>body{font-family:var(--rs-font)}</code>; (3) класс <code>.rs-table</code> для справочных таблиц (sticky thead, hover, <code>.num</code> с tabular-nums).',
      'ROADMAP — добавлена подфаза 1.26 «Единый дизайн всех модулей» с 9 пунктами: полноэкранное отображение (готово), типографика (готово), rs-table (готово), плюс backlog на единые статусные токены, формы, шапки, badge-ы, breadcrumb, аудит-скрипт.',
    ],
  },
  {
    version: '0.1.20',
    date: '2026-04-22',
    items: [
      'Phase 1.24.40 — уникальность идентификации шкафов в проекте. (1) Тег TIA-942 (поле в topbar) — нельзя присвоить двум стойкам одно обозначение: проверка на change, при коллизии откат на прежнее значение + toast с указанием какой стойке уже принадлежит этот тег. (2) Обозначение (name) шаблона стойки в rack-config также должно быть уникальным: readForm() при попытке переименовать в занятое имя откатывает поле и показывает toast. При создании новой стойки (addTemplate / duplicate) имя автоматически дополняется « (2)», « (3)» до уникального.',
      'Phase 1.24.40 — концепция уточнена: «шаблон стойки» в rack-config относится только к физическому шкафу (корпус + принадлежности + PDU). IT-оборудование размещается в scs-config/rack.html поверх этого корпуса. Подпись в topbar rack.html: «Физический шкаф» (с title-подсказкой). Blank-template теперь называется «Новый шкаф» (было «Новый шаблон»).',
      'Phase 1.24.40 — основная карта юнитов в rack.html получает zoom/pan (как раньше только в модалке): wheel = зум у курсора 0.4…5×, drag по пустому месту = pan через scrollLeft/scrollTop. renderUnitMap(\'sc-unitmap\', { big: true }) — раньше big:false.',
    ],
  },
  {
    version: '0.1.19',
    date: '2026-04-22',
    items: [
      'Phase 1.24.39 — в rack.html добавлен левый сайдбар (220px) со списком всех шкафов проекта. Каждая карточка: тег TIA-942, название, мини-полоса занятости (зелёный / жёлтый / красный), кол-во устройств, %. Клик по карточке → переключение state.currentRackId + history.replaceState(?rackId=…) без перезагрузки страницы. Активная стойка подсвечена синим. Ссылка «все →» ведёт обратно в index (реестр).',
      'Phase 1.24.39 — в inventory.html добавлена подсветка строк по регламенту ТО: <span style="background:#fef2f2">красноватая</span> если <code>lastMaintAt + maintMonths × 30 дней</code> уже прошёл (просрочка), <span style="background:#fffbeb">жёлтая</span> если до конца регламента ≤ 14 дней или регламент задан, но даты последнего ТО ещё нет. Помогает быстро увидеть, что нужно обслужить.',
      'CSS — .sc-grid-3 (220/1fr/1.2fr), адаптив: на экранах < 1280px сайдбар усаживается до 180px и preview переносится на всю ширину.',
    ],
  },
  {
    version: '0.1.18',
    date: '2026-04-22',
    items: [
      'Phase 1.24.38 — разделение модуля СКС на 3 страницы согласно целевой концепции: (1) <code>index.html</code> — «Шкафы проекта» (реестр всех шкафов, только просмотр, без drag; клик по строке → rack.html?rackId=…); (2) <code>rack.html</code> — сам конфигуратор конкретного шкафа (большое окно, каталог, тележка, склад, матрица, BOM, drag-n-drop юнитов); (3) <code>inventory.html</code> — «Реестр оборудования» (все устройства проекта: в шкафах + тележка + склад, с редактируемыми полями S/N, статус, регламент ТО, заметки, CSV-экспорт).',
      'Phase 1.24.38 — между страницами добавлена breadcrumb-навигация (.sc-breadcrumb). Главное окно СКС больше не занимается перетаскиванием юнитов — только обзор; редактирование шкафа делается на отдельной странице, схема СКС/монтаж/склад остались для будущих модулей (inter-rack-scs).',
      'Phase 1.24.38 — модалка полноэкранного просмотра удалена из rack.html (она и так полноэкранная). Старый код открытия модалки в scs-config.js оставлен как no-op (guard по отсутствию #sc-unitmap-fullscreen / #sc-unitmap-dlg).',
      'Phase 1.24.38 — inventory.html: статусы (🟢 активен / 🟡 резерв / 🛠 обслуживание / ⚫ списан), регламент ТО (месяцев между ТО), дата последнего ТО. Редактирование inline — change-события пишут обратно в тот же LS-ключ (LS_CONTENTS для rack / LS_CART / LS_WAREHOUSE).',
    ],
  },
  {
    version: '0.1.17',
    date: '2026-04-22',
    items: [
      'Phase 1.24.37 — «отрыв» устройства от шкафа при pointer-drag. Теперь интра-драг полностью повторяет UX перетаскивания из тележки/склада: на pointerdown полоса устройства скрывается (display:none), пользователь видит только плавающий ghost у курсора и dashed-превью target-U в шкафу (синий=валидно, красный=занято). Позиция устройства коммитится ТОЛЬКО на pointerup — до этого DOM-модель не меняется, поэтому не мелькает промежуточных rerender-ов. На pointercancel / drop в cart/wh / drop вне валидной зоны — откат через rerenderPreview (полоса возвращается на место).',
      'Phase 1.24.37 — wantU считается по текущей позиции курсора внутри svg (elementFromPoint → closest(\'svg.sc-unitmap-svg\')), а не по delta от startY. Поэтому можно drag\'ом перетаскивать устройство между основной картой и модалкой в одном жесте: превью показывается в том svg, где сейчас курсор. Используются dataset.rowh/.bodyw/.bodyx/.zoom текущего svg.',
    ],
  },
  {
    version: '0.1.16',
    date: '2026-04-22',
    items: [
      'Phase 1.24.36 доводка — единый UX drag для pointer-drag (внутри стойки) и HTML5 DnD (каталог/тележка/склад). При pointer-drag теперь появляется такой же плавающий ghost (.sc-drag-ghost-live) у курсора с цветом type.color и подписью «Название · NU», следит за курсором в pointermove, удаляется в pointerup/pointercancel. Прозрачность перетаскиваемой полосы изменена с 0.75 на 0.55 (заметнее).',
      'Phase 1.24.36 доводка — live-превью target-U при pointer-drag. Синий dashed rect когда позиция валидна, красный dashed rect когда wantU занят другим устройством (canPlace=false). Устройство при этом остаётся на последней валидной позиции — как раньше, но пользователь сразу видит, ПОЧЕМУ оно не двигается дальше. Превью скрывается при наведении курсора на тележку/склад.',
    ],
  },
  {
    version: '0.1.15',
    date: '2026-04-22',
    items: [
      'Phase 1.24.24 базово — URL-роутинг. Поддержка ?rackId=<id> и ?tag=<TIA> в query (напр. ?tag=DC1.H3.R05 открывает стойку с этим тегом). Смена стойки в picker обновляет URL через history.replaceState — можно шарить ссылку на конкретную стойку. Без параметров — поведение прежнее (auto-pick первой стойки).',
      'Fix — live-превью target-U в SVG использует dataset.bodyw / dataset.bodyx вместо вычисления из svg.getAttribute(\'width\'). Раньше в модалке ширина превью ошибочно включала extraRight=120 под кривые кабелей. Теперь превью точно совпадает с реальной шириной полосы устройства.',
    ],
  },
  {
    version: '0.1.14',
    date: '2026-04-22',
    items: [
      'Fix — drag из тележки/склада в стойку (в модалке и в основной). Причина: dropEffect=\'move\' при effectAllowed=\'copy\' (каталог) блокировал drop по HTML5-спеке. Теперь dropEffect выбирается по источнику: typeId→copy, cartId/whId→move.',
      'Fix — bindCartWarehouseDropzones теперь привязывает ВСЕ .sc-cart-dropzone / .sc-wh-dropzone (и основные, и в модалке), а не только первые по querySelector.',
      'Phase 1.24.36 — визуальный ghost при HTML5-drag. setDragGhost(ev, type, label) создаёт цветной div с названием и высотой (H U), цвет фона = type.color, помещает в document.body и передаёт в dataTransfer.setDragImage. Ghost повторяет вид полосы в стойке. Вызывается из dragstart для каталога, тележки и склада.',
      'Phase 1.24.36 — live-превью target-U при dragover на SVG. Полупрозрачный (fill-opacity 0.25) синий прямоугольник с пунктирной рамкой в строке под курсором, высота = heightU dragged-устройства. Обновляется в реальном времени. state._dragMeta хранит { h, label, color } на время drag. Убирается на dragleave/drop.',
      'UI — убран padding у .sc-zoomwrap (0 вместо 8px), чтобы исключить зоны вне SVG, которые не ловят drop. Drop теперь надёжно срабатывает по всей площади карты.',
    ],
  },
  {
    version: '0.1.13',
    date: '2026-04-22',
    items: [
      'Phase 1.24.35 доводка — автонумерация адресов + поиск + CSV инвентаря. cartToWarehouse теперь назначает item.address = suggestNextAddress() (увеличение последнего числового сегмента последнего адреса; стартовый A-01-1-1). Поисковая строка в секции Склад: фильтрует по адресу/названию/S/N/заметке; refocus через requestAnimationFrame после re-render. Кнопка CSV в секции Склад — экспортирует инвентарь (адрес, устройство, S/N, заметка, стойка-источник, дата поступления) с сортировкой по адресу natural.',
    ],
  },
  {
    version: '0.1.12',
    date: '2026-04-22',
    items: [
      'Убрана иконка 🛒 с полос устройств на карте (drag-on-cart работает напрямую). Теперь на карте чисто — только полосы и метки.',
      'Phase 1.24.32 доводка — drag со склада напрямую в стойку. application/x-scs-whid добавлен в accept-список SVG drop-zone (bindUnitMapDrop). Путь: warehouseToCart → последний элемент тележки → installFromCart(cart[-1].id, wantTopU). Drag с карты прямо на склад: pointerup при overWh = moveToCart + cartToWarehouse.',
      'Phase 1.24.33 — тележка и склад в полноэкранной модалке. Модалка разделена на 2 колонки: карта слева (flex:1) + боковая панель 280px с мини-тележкой и мини-складом справа. Оба — полноценные .sc-cart-dropzone / .sc-wh-dropzone, принимают drag с карты модалки через elementFromPoint. renderCart/renderWarehouse теперь рендерят в оба хоста (main + dlg). Новые id: #sc-cart-dlg #sc-wh-dlg #sc-cart-badge-dlg #sc-wh-badge-dlg.',
      'Phase 1.24.33 — zoom + pan карты в модалке. Wheel на .sc-zoomwrap — зум 0.4×–5× с якорем в точке курсора (сохраняется видимая точка при изменении масштаба). Drag по пустому месту карты (не по полосе устройства) — pan через scrollLeft/scrollTop с setPointerCapture. state.dlgZoom хранит текущий зум между перерисовками. svg.setAttribute width/height * zoom вместо CSS transform (нативный скролл, корректные координаты). Drag устройств учитывает zoom: drows = -dy / (rowH * zoom).',
      'Phase 1.24.34 — умная авто-укладка по правилам размещения. PRIO = { patch:5, switch:15, kvm:35, monitor:40, server:55, other:60, organizer:70, ups:95 }. Зоны top(<25)/middle/bottom(>80). Патч-панели и коммутаторы — сверху (ввод кабелей с верхнего лотка), KVM/монитор — середина (уровень глаз), серверы — середина, ИБП — в самый низ (низкий центр тяжести, короткие силовые до PDU). 1U зазор между зонами для вентиляции. Органайзеры как разделители между разными kind-ами в middle-зоне. Крупные серверы выше внутри middle, крупные ИБП ниже внутри bottom.',
      'Phase 1.24.35 — адресное хранение на складе. item.address (свободная строка «зона-стеллаж-полка-ячейка», напр. A-12-3-2). Кнопка 📍 → scPrompt, с проверкой дубликата адреса (warn с ок/отмена). Новая колонка «Адрес» первой в таблице склада (код-стиль, жёлтый фон). Сортировка по адресу localeCompare numeric (A-2 < A-10), пустые адреса в конец, в пределах равного адреса — по storedAt desc.',
    ],
  },
  {
    version: '0.1.11',
    date: '2026-04-22',
    items: [
      'Phase 1.24.32 доводка — инвентарь на складе. item.storedAt (timestamp) ставится при cartToWarehouse и сбрасывается при warehouseToCart. item.serial (S/N) и item.note (заметка) редактируются через editWarehouseItem → scPrompt x2. Таблица Склада перестроена: колонки [Устройство][S/N · заметка][Было в][Хранится][Действия]. Сортировка по storedAt desc (новые сверху). Относительный возраст через fmtAge (сегодня / N дн. / N мес. / N г.) с tooltip = точная дата/время. Кнопка 📝 для редактирования S/N + заметки.',
    ],
  },
  {
    version: '0.1.10',
    date: '2026-04-22',
    items: [
      'Phase 1.24.28 доработка — тележка теперь визуально похожа на тележку: пунктирная рамка с скруглением 6/14px, «колёсики» снизу (два круга radial-gradient, absolute bottom:-8px), иконка 🛒. Drag-from-rack-to-cart через pointer events + document.elementFromPoint: во время pointermove проверяется .sc-cart-dropzone под курсором, устанавливается state.drag.overCart, устройство «замирает» пока курсор над тележкой; на pointerup — moveToCart(devId) если overCart.',
      'Phase 1.24.32 базово — Склад (Warehouse). LS_WAREHOUSE=scs-config.warehouse.v1 для долговременного хранения неиспользуемого оборудования. Секция 📦 Склад в правой колонке (solid yellow border). Операции cartToWarehouse/warehouseToCart/discardWarehouseItem. HTML5 DnD cross-drag: тележка↔склад через разные mime-типы (application/x-scs-cartid для cart→wh, application/x-scs-whid для wh→cart). На складе строки draggable + кнопки «→ тележка» и «✕ удалить» (scConfirm). Визуальный feedback .sc-drop-hover (синий outline + scale 1.01).',
    ],
  },
  {
    version: '0.1.9',
    date: '2026-04-22',
    items: [
      'Phase 1.24.23 базово — TIA-942 тег стойки. Поле «Тег стойки (TIA-942)» в topbar (напр. DC1.H3.R05, maxlength 24). Хранится в LS_RACKTAGS=scs-config.rackTags.v1 как {[rackId]: tag}. При смене стойки input подгружает её тег.',
      'Phase 1.24.30 базово — авто-теги устройств (TIA-606). deviceTag(d) = <rackTag>.U<top> для 1U или <rackTag>.U<top>-<bottom> для многоюнитных. Показан в подписи полосы на карте юнитов (СКС и Питание режимы), в таблице «Размещение» (новая колонка «Тег», моноширинная 11px), в CSV экспорте отдельной секцией «Теги устройств TIA-606» с сортировкой сверху вниз.',
    ],
  },
  {
    version: '0.1.8',
    date: '2026-04-22',
    items: [
      'Phase 1.24.28 базово — «тележка» (moving cart). Межстоечный буфер как в реальном ЦОД. LS_CART=scs-config.cart.v1 общий для всех стоек. Секция 🛒 Тележка (N) с бейджем в правой колонке. Иконка 🛒 в каждой полосе карты — клик вытаскивает устройство на тележку (pduOutlet сбрасывается, fromRackId/fromRackName запоминаются). Drag строки тележки на карту любой стойки = installFromCart → findNearestFreeSlot от wantU. Кнопки «↩ вернуть» (переключиться в исходную стойку, поставить, вернуться на текущую — прозрачно для пользователя) и «✕ выгрузить на склад» (удалить с подтверждением scConfirm). Storage сохраняется между сессиями.',
    ],
  },
  {
    version: '0.1.7',
    date: '2026-04-22',
    items: [
      'UI — шкаф в модалке по центру. .sc-unitmap-dlg-body: align-items:center + margin:auto для svg + центрирование легенды.',
    ],
  },
  {
    version: '0.1.6',
    date: '2026-04-22',
    items: [
      'UI fix — вернул полный шкаф (рамка + все U + нумерация) на маленькой карте. Ранее по ошибке на маленькой карте скрывались пустые ряды. Теперь маленькая и большая карта одинаково показывают шкаф; отличие только — патч-корды рисуются исключительно в модалке.',
    ],
  },
  {
    version: '0.1.5',
    date: '2026-04-22',
    items: [
      'UX — полный отказ от браузерных alert/confirm/prompt. Введён in-page слой: scToast(msg, kind) с 4 вариантами (info/ok/warn/err, auto-hide 3-5 сек, fade-in/out в правом нижнем углу) и scConfirm(title, msg, {okLabel, cancelLabel, input}) → Promise, scPrompt(title, default) поверх scConfirm. Заменены все 8 точек (удаление типа, addToRack без стойки, drop без места, save/apply пресета, reset каталога, shrink при применении). Esc/клик по подложке = отмена, Enter в prompt = OK.',
    ],
  },
  {
    version: '0.1.4',
    date: '2026-04-22',
    items: [
      'Phase 1.24.29 базово — запрет наложения при drag-n-drop. canPlace(r, devices, excludeDevId, h, wantU) проверяет rack-occupied + других устройств; drag упирается в препятствие (d.positionU не меняется, не продвигается сквозь); drop из каталога ищет ближайший свободный слот через findNearestFreeSlot (вверх по U, потом вниз). Если стойка заполнена — alert.',
      'Phase 1.24.12 — fix drag в модалке. В state.drag теперь svgId, после обновления позиции rerenderPreview() перерисовывает и основную карту и модалку, re-capture g делается в том же svg (по svgId), где начался drag.',
      'Phase 1.24.12 detail — разное отображение на маленькой/большой карте. Маленькая: только заполненные юниты + rack-occupied, без пустых рамок и без нумерации свободных U (режим обзора). Большая (модалка): все U с нумерацией + пустые рамки + патч-корды из СКС-матрицы (кривые Безье справа от стойки, цвет по типу кабеля: cat.6 синий, OM оранжевый, OS жёлтый; + цвет из link.color если задан). Корды соединяют устройства по префикс-совпадению между link.a/b и device.label.',
    ],
  },
  {
    version: '0.1.3',
    date: '2026-04-22',
    items: [
      'Phase 1.24.10 базово — drag-n-drop из каталога в карту юнитов. Строки таблицы каталога получили draggable=true + dragstart(application/x-scs-typeid). Карта (обычная и в модалке) приняла dragover/drop: при сбросе вычисляется U по clientY (с учётом scale и viewBox), устройство создаётся в этом U (clamp по heightU). Визуальный фидбэк: курсор grab на строках, opacity 0.55 для тащимой строки, outline+фон на SVG при hover.',
    ],
  },
  {
    version: '0.1.2',
    date: '2026-04-22',
    items: [
      'Phase 1.24.11 — два режима отображения карты юнитов. Переключатель «🧵 СКС / ⚡ Питание» справа от заголовка карты. В режиме СКС полосы красятся по типу оборудования (как раньше). В режиме «Питание» — по вводу PDU: A=синий, B=красный, C=зелёный, D=фиолетовый, «без PDU» — серый + иконка ⚠. Легенда перестраивается под режим. Подписи устройств в режиме «Питание» показывают «ввод X · Y Вт».',
      'Phase 1.24.12 — полноэкранная карта в <dialog>. Кнопка «⛶ Во весь экран» открывает модалку 96vw × 92vh, полосы увеличены вдвое (rowH=32, bodyW=440), свой переключатель режима и кнопка «✕ Закрыть». Dnd работает внутри модалки. Файлы: scs-config/{index.html(+dialog, +head с toggle), scs-config.js(renderUnitMap(hostId, {big}) + viewMode в state + feedColor + bindUnitMapDrag(svgId)), scs-config.css(+60 строк: .sc-viewmode, .sc-unitmap-dlg, .sc-unitmap-dlg-bar, .sc-unitmap-dlg-body)}.',
    ],
  },
  {
    version: '0.1.1',
    date: '2026-04-22',
    items: [
      'Phase 1.24.3 full — drag-n-drop в SVG-карте юнитов. Pointer Events API с setPointerCapture, snap к целому U, визуальная обратная связь (cursor=grabbing, opacity=0.75). На pointerup коммит в contents + rerender.',
      'Phase 1.24.4 full — интеграция с розетками PDU из rack-config. Dropdown `pduFeed` из уникальных вводов стойки (A/B/C/…), dropdown `pduOutlet` из развёрнутых rack.pdus[].outlets (тип + номер, с учётом qty инстансов PDU). Занятые другими устройствами розетки показываются disabled. Hard-check перегруза по каждому вводу: сумма powerW vs rating·√3(для 3ф)·230·cosφ·qty. Детекция дублирования розеток.',
      'Phase 1.24.7 — «готовая сборка» как шаблон. Новое хранилище `scs-config.assemblyTemplates.v1` = [{id, name, contents, matrix, createdAt}]. Кнопки «💾 Сохранить как шаблон» и «↪ Применить» в верхней панели. Применение клонирует с новыми id и обрезает устройства, не помещающиеся в меньшую стойку (с уведомлением).',
    ],
  },
  {
    version: '0.1.0',
    date: '2026-04-22',
    items: [
      'Phase 1.24 MVP — первая рабочая версия модуля. Ранее была stub-страница с описанием. Теперь: выбор стойки из шаблонов rack-config (localStorage rack-config.templates.v1), каталог типов оборудования (коммутатор / патч-панель / сервер / KVM / монитор / ИБП-1U / органайзер / другое) с правкой и цветом, размещение по U с автоматическим поиском первой свободной области, детекция конфликтов (наезд на соседа / на «занятые» юниты rack-config / выход за границы), SVG-карта фронт-вью с цветными полосами, предупреждения (превышение U, превышение мощности vs rack.demandKw, непривязанные к PDU устройства), СКС-матрица (порт↔порт) с типом кабеля и длиной, BOM по оборудованию + патч-кордам + CSV. Авто-укладка сверху вниз без зазоров. Файлы: scs-config/{index.html,scs-config.js,scs-config.css,changelog.js} (новые).',
    ],
  },
];
