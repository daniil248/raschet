// =========================================================================
// js/methods/pue-rk.js — методика РК (Правила устройства электроустановок
// Республики Казахстан / СН РК 4.04). D5: казахстанская норма как
// ОТДЕЛЬНЫЙ метод-файл (отключаемый/версионируемый независимо).
// Электрика РК восходит к ПУЭ-линии (как ПУЭ 7 РФ) → ядро методики
// переиспускается из pue.js (...pue); здесь — собственная META
// (норма/регион/версия) для независимой дивергенции/версии без правки
// РФ-метода. Аддитивно: id 'pue-rk' — старые сохранённые расчёты по
// 'pue' не затрагиваются (getMethod fallback сохранён).
// =========================================================================

import pue from './pue.js';

export default {
  ...pue,
  id: 'pue-rk',
  label: 'ПУЭ РК',
  standard: 'Правила устройства электроустановок РК / СН РК 4.04',
  region: 'KZ',
  version: '1.0',
  enabled: true,
  discipline: 'electrical',
};
