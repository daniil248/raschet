/* =========================================================================
   hvac-methods/air-balance-sprk.js — воздухообмен по нормам Республики
   Казахстан (СН РК 4.02-01 / СП РК 4.02-101, Отопление, вентиляция и
   кондиционирование). D5: казахстанская норма как ОТДЕЛЬНАЯ методика-
   файл (отключаемая/версионируемая независимо). Методика воздухообмена
   РК — той же СНиП-линии, что и СП 60.13330 РФ → расчётное ядро
   переиспользуется из air-balance.js; здесь — собственная META для
   независимой дивергенции/версии.
   ========================================================================= */

import { compute as _computeRu, META as _ruMeta } from './air-balance.js';

export const META = {
  id: 'air-balance-sprk',
  label: 'Воздухообмен — СН РК 4.02-01 / СП РК 4.02-101',
  standard: 'СН РК 4.02-01-2011 / СП РК 4.02-101',
  region: 'KZ',
  version: '1.0',
  enabled: true,
  discipline: 'hvac',
  refs: ['СН РК 4.02-01', 'СП РК 4.02-101', 'СНиП-линия (как СП 60.13330 РФ)'],
  inputs: _ruMeta.inputs,
};

export function compute(input = {}) {
  const r = _computeRu(input);
  return { ...r, method: META.id, standard: META.standard };
}
