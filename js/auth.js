/* =========================================================================
   js/auth.js — DEPRECATED. Логика переехала в shared/auth.js, чтобы
   подпрограммы могли подключать один и тот же модуль автономно.
   Этот файл оставлен как no-op shim для обратной совместимости: если
   index.html ещё ссылается на него, ничего не произойдёт (реальная
   инициализация выполняется shared/auth.js, подключаемым первым).
   ========================================================================= */
if (typeof window.Auth === 'undefined') {
  console.warn('[auth] js/auth.js загружен без shared/auth.js — проверьте index.html (ожидается <script src="shared/auth.js">)');
}
